name: PR Review Bot

on:
  pull_request:
    # Stacked-PR support: a PR whose BASE is another claude/* feature branch (the
    # staged-stack review flow in CLAUDE.md) must run the same gates as a PR to
    # main — without this, only the bottom of a stack ever got CI.
    branches: [main, 'claude/**']
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  pull-requests: write
  checks: write

jobs:
  eslint-reviewdog:
    name: ESLint reviewdog
    runs-on: ubuntu-latest
    if: ${{ github.event.pull_request.draft == false }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Review JavaScript changes
        uses: reviewdog/action-eslint@v1.34.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          reporter: github-pr-review
          filter_mode: added
          fail_level: none
          level: warning
          eslint_flags: "."
