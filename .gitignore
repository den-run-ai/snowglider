# Dependencies
node_modules/

# IDE files
.DS_Store
.vscode/

# Debug logs
npm-debug.log*

# Test results (generated by CI)
test-results/

# Playwright HTML report (generated by `npm run test:e2e`)
playwright-report/

# Visual-regression baselines (npm run test:e2e:visual). They are
# environment-specific (browser build + host system fonts) AND the repo keeps
# `main` text-only / binary-free per CLAUDE.md, so baseline PNGs are NOT committed:
# each environment generates its own with `npm run test:e2e:visual:update`.
tests/e2e/visual/**/*-snapshots/

# GitHub Pages build output
dist/

# Coverage reports (anchored to the repo-root output dir so it does not also
# ignore the tests/coverage/ tooling sources)
/coverage/

# Large video files
*.mov
*.mp4

# Local-only security notes (not for publication)
SECURITY.md

# Firebase emulator debug logs (produced by `npm run test:firebase`)
firestore-debug.log
firebase-debug.log
ui-debug.log
